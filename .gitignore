.env
.env.*
scripts/.venv/
.DS_Store

# Laufzeit-/Spieldaten (per Volume gemountet) — gehören NICHT ins Repo
# serverfiles/ = Server-Installation (~3 GB, regenerierbar — wird per SteamCMD neu geladen)
# config/ = Savegames + Servereinstellungen (separat sichern, siehe SETUP-MAC.md)
serverfiles/
config/
# Altbestand (früherer Name der Installation), falls noch vorhanden:
satisfactory/
